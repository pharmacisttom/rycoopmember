import { StatePanel } from "../_components/state-panel";

export default function NotificationsPage() {
  return (
    <StatePanel title="การแจ้งเตือน">
      <div className="rounded-md bg-cyan-50 p-3 font-semibold text-navy-900">มีข้อความตอบกลับจากเจ้าหน้าที่ 2 รายการ</div>
    </StatePanel>
  );
}
